<?php
    // Le thème utilisé
    $config['theme'] = 'defaut';
    // Le temps (en minutes) entre chaque requête
    $config['verif_time'] = 20;
    // Temps, en microsecondes, entre chaque requête
    $config['between'] = 0;
?>